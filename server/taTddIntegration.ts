/**
 * TA/TDD Integration (Deprecated)
 * 
 * This file is deprecated. Use project-table-provisioner.ts instead.
 * Tables are now provisioned using the standard schema from db-project-schema.sql
 * with table prefixes (proj_{id}_*) in the shared database.
 */

export {}; // Make this a module
